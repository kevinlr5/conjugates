machine:
  services:
    - docker
  java:
    version:
      oraclejdk8
dependencies:
  pre:
    - "[[ ! -s \"$(git rev-parse --git-dir)/shallow\" ]] || git fetch --unshallow"
  override:
    - echo 'skipping dependencies'
test:
  override:
    - ./gradlew clean check
